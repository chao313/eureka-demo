##指定应用名 - 注意! 在CloudConfig的项目中，必须使用bootstrap!!!
spring:
  application:
    name: config-client
  cloud:
    config:
      label: master #git的版本
      uri: http://192.168.0.102:9999 #config-server的地址
      name: config-info #应用名称
      profile: dev #偏好