server:
  port: 9999


##指定注册中心
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

##指定应用名
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/chao313/config-server
          username: chao313
          password: Ys20140913
          search-paths: config_dir
          #basedir: gitwork #指定git的work地址









